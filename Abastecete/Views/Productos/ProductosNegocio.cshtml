@model BusinessLogic.Models.NegocioPersona

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <link rel="icon" type="image/x-icon" href="~/css/Images/Logo A naranja.png" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
    <link rel="stylesheet" href="~/css/styles.css" asp-append-version="true" />
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body class="bg-white">
    <header class="relative w-full bg-[#F5EFE4] shadow-md">
        <div class="relative h-56 w-full">
            <img src="/ruta/a/la/imagen/de/fondo.jpg" alt="Fondo"
                 class="absolute inset-0 h-full w-full object-contain brightness-50" />
            <div class="relative top-[69%] z-10 flex flex-row items-start gap-4 px-4 sm:top-24 md:items-start md:gap-6 md:px-10">
                <div class="flex size-32 items-center justify-center overflow-hidden rounded-xl border bg-white shadow-lg sm:size-60">
                    @if (!string.IsNullOrWhiteSpace(Model.Negocio?.imagen?.Base64))
                    {
                        <img src="@Model.Negocio.imagen.Base64" alt="Logo negocio" class="h-full w-full object-cover" />
                    }
                </div>

                <div class="absolute top-1/2 left-44 -translate-y-1/2 text-left sm:left-72">
                    <h1 class="text-3xl leading-tight font-extrabold tracking-tight text-gray-800 sm:text-4xl">
                        @Model.Negocio?.Nombre
                    </h1>
                    <p class="text-base font-semibold tracking-wide text-[#15803D] uppercase sm:text-lg">
                        @Model.Negocio?.Telefono
                    </p>
                </div>  
            </div>
        </div>
    </header>


    <div id="mensaje-modal" class="fixed inset-0 z-50 flex hidden items-center justify-center bg-black/50">
        <div class="w-full max-w-md rounded-lg p-6 shadow-lg" style="background-color: #F4F1E9;">
            <div class="flex justify-center">
                <img src="~/css/Images/LOGO FINAL.png" alt="Abastecete Logo" class="h-12">
            </div>
            <h2 class="mt-2 text-center text-xl font-bold text-[#23916A]">Solicitud</h2>

            <form id="mensaje-form" class="mt-4">
                <input type="email" id="email" name="email" value="@Model.Persona.Correo"
                       class="mb-3 w-full rounded-lg border border-gray-300 bg-gray-100 p-3 text-black" required>

                <div class="mb-3 flex items-center space-x-2">
                    <span class="flex items-center rounded-lg border border-gray-300 bg-white px-4 py-3">
                        +57
                    </span>
                    <input type="text" id="phone" name="phone" value="@Model.Negocio.Telefono"
                           class="w-full rounded-lg border border-gray-300 bg-gray-100 p-3 text-black" required>
                </div>

                <textarea id="message" name="message" placeholder="Escribe tu solicitud..."
                          class="mb-3 h-32 w-full resize-none rounded-lg border border-gray-300 bg-gray-100 p-3 text-black"
                          required></textarea>

                <button type="submit" class="w-full rounded-lg bg-[#23916A] py-3 font-bold text-white hover:bg-[#1E7B57]">
                    Enviar solicitud
                </button>
            </form>

            <button id="close-mensaje-modal" class="mt-3 block w-full text-center font-medium text-black">
                Cerrar
            </button>
        </div>
    </div>

    <main class="mt-32 w-full px-4 sm:px-6 lg:px-10">
        <div class="w-full transition-all">
            <div id="productosWrapper"
                 class="w-full rounded-lg border border-gray-300 bg-white p-4 shadow-md transition-all duration-300 sm:p-6">

                <div class="flex items-center gap-2">
                    <!-- Botón Mensaje -->
                    <a href="#" onclick="openMensajeModal()"
                       class="flex h-10 w-10 items-center justify-center rounded-md bg-gray-600 text-white transition hover:bg-gray-700">
                        <i class="fas fa-comment-dots text-sm"></i>
                    </a>

                    <!-- Botón Agregar Producto -->
                    <a href="@Url.Action("AgregarProductNegocio", "Productos")"
                       class="flex h-10 w-10 items-center justify-center rounded-md bg-orange-500 text-white transition hover:bg-orange-600">
                        <i class="fas fa-plus text-sm"></i>
                    </a>

                    <!-- Botón Editar Negocio -->
                    <a href="@Url.Action("EditarNegocio","Negocios")"
                       class="flex h-10 w-10 items-center justify-center rounded-md bg-orange-500 text-white transition hover:bg-orange-600">
                        <i class="fas fa-pen-to-square text-sm"></i>
                    </a>
                </div>




                <!-- Grid de productos -->
                <div id="productosContainer"
                     class="grid grid-cols-1 gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5">
                    @foreach (var producto in ViewBag.productos)
                    {
                        <div id="producto-@producto.Id"
                             class="producto relative flex flex-col rounded-lg border bg-white p-4 shadow-md"
                             data-categoria-id="@producto.IdSubCategoria" data-precio="@producto.Precio">
                            <img src="@producto.ImagenUrl" alt="@producto.Nombre"
                                 class="mb-4 h-40 w-full object-contain" />
                            <h3 class="text-lg font-semibold">@producto.Nombre</h3>
                            <p class="text-xl font-bold text-orange-500">$@producto.Precio</p>
                            <button onclick="mostrarDetalle('@producto.Nombre', '@producto.Precio', '@producto.ImagenUrl', '@producto.Id')"
                                    class="mt-4 self-center rounded-lg bg-[#1A2D3D] px-6 py-2 text-white hover:bg-[#162331]">
                                Ver
                            </button>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Detalle lateral del producto -->
        <div id="detalleProducto"
             class="fixed right-0 hidden translate-x-full transform rounded-tl-2xl bg-white p-6 shadow-lg transition-all duration-300 md:block md:w-[400px]"
             style="top: calc(100px + 4rem); height: calc(100vh - 100px - 4rem);">

            <button onclick="ocultarDetalle()"
                    class="absolute top-4 right-4 text-gray-700 transition duration-200 hover:text-red-500">
                <i class="fas fa-times-circle text-2xl"></i>
            </button>

            <div class="flex h-64 w-full items-center justify-center rounded-md bg-gray-300">
                <img id="detalleImagen" class="h-full w-full object-contain" src="" alt="Imagen del producto" />
            </div>
            <h3 id="detalleNombre" class="mt-4 text-2xl font-bold"></h3>
            <p id="detallePrecio" class="text-xl font-bold text-green-600"></p>
            <div class="mt-4 flex gap-2">
                <button class="w-full rounded-lg bg-green-600 px-6 py-2 text-white">Contáctanos</button>
            </div>
        </div>
    </main>


    <script>
        // Definir la función en el ámbito global
        function openMensajeModal() {
            const mensajeModal = document.getElementById("mensaje-modal");
            if (mensajeModal) {
                mensajeModal.classList.remove("hidden");
                document.body.classList.add("overflow-hidden");
            }
        }

        function closeMensajeModal() {
            const mensajeModal = document.getElementById("mensaje-modal");
            if (mensajeModal) {
                mensajeModal.classList.add("hidden");
                document.body.classList.remove("overflow-hidden");
            }
        }

        document.addEventListener("DOMContentLoaded", function () {
            const mensajeModal = document.getElementById("mensaje-modal");
            const closeModalBtn = document.getElementById("close-mensaje-modal");
            const mensajeForm = document.getElementById("mensaje-form");

            // Manejo del botón de cierre
            if (closeModalBtn) {
                closeModalBtn.addEventListener("click", closeMensajeModal);
            }

            // Cerrar modal al hacer clic fuera de ella
            window.addEventListener("click", function (e) {
                if (e.target === mensajeModal) {
                    closeMensajeModal();
                }
            });

            // Manejo del envío del formulario
            if (mensajeForm) {
                mensajeForm.addEventListener("submit", async function (e) {
                    e.preventDefault();

                    const formData = new FormData(mensajeForm);

                    try {
                        const response = await fetch("/api/contact/send", { // Usa la misma ruta
                            method: "POST",
                            body: formData
                        });

                        const result = await response.json();

                        if (result.success) {
                            alert("Mensaje enviado correctamente.");
                            mensajeForm.reset();
                            closeMensajeModal();
                        } else {
                            alert("Error al enviar el mensaje.");
                        }
                    } catch (error) {
                        console.error("Error enviando el mensaje:", error);
                        alert("Hubo un problema al enviar el mensaje.");
                    }
                });
            }
        });

    </script>
    <script>
        // Definir las variables en el ámbito global
        let productoSeleccionado = null;
        let detalleAbierto = false;

        function mostrarDetalle(nombre, precio, imagen, id) {
            const detalleProducto = document.getElementById('detalleProducto');
            const productosWrapper = document.getElementById('productosWrapper');
            const productosContainer = document.getElementById('productosContainer');

            // Mostrar el detalle del producto
            detalleProducto.classList.remove('hidden', 'translate-x-full');
            detalleProducto.classList.add('w-1/5', 'translate-x-0');

            productosWrapper.classList.remove('w-5/5');
            productosWrapper.classList.add('w-4/5');

            productosContainer.classList.remove('lg:grid-cols-4');
            productosContainer.classList.add('lg:grid-cols-3');

            document.getElementById('detalleNombre').textContent = nombre;
            document.getElementById('detallePrecio').textContent = `$${precio}`;
            document.getElementById('detalleImagen').src = imagen;

            // Ocultar el producto seleccionado antes de mostrar el detalle
            if (productoSeleccionado) {
                productoSeleccionado.classList.remove('hidden');
            }

            productoSeleccionado = document.getElementById(`producto-${id}`);
            productoSeleccionado.classList.add('hidden');

            detalleAbierto = true;
        }

        function ocultarDetalle() {
            const detalleProducto = document.getElementById('detalleProducto');
            const productosWrapper = document.getElementById('productosWrapper');
            const productosContainer = document.getElementById('productosContainer');

            // Ocultar el detalle del producto
            detalleProducto.classList.add('hidden', 'translate-x-full');
            detalleProducto.classList.remove('w-1/5', 'translate-x-0');

            productosWrapper.classList.remove('w-4/5');
            productosWrapper.classList.add('w-5/5');

            productosContainer.classList.remove('lg:grid-cols-3');
            productosContainer.classList.add('lg:grid-cols-4');

            if (productoSeleccionado) {
                productoSeleccionado.classList.remove('hidden');
                productoSeleccionado = null;
            }

            detalleAbierto = false;
        }
    </script>

   
</body>
</html>