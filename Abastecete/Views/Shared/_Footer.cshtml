<!-- Footer Premium -->
<footer class="mt-16 bg-gray-900">
    <!-- Contenido principal -->
    <div class="pt-12 pb-8">
        <div class="max-w-7xl mx-auto px-4">

            <!-- Grid principal -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 sm:gap-8 mb-10 sm:mb-12">

                <!-- Logo y descripción -->
                <div>
                    <a href="@Url.Action("Principal", "Home")" class="inline-block mb-6">
                        <img src="~/images/logo-final.png" alt="Abastecete" class="h-12 w-auto" style="filter: brightness(0) invert(1);" />
                    </a>
                    <p class="text-gray-400 text-sm leading-relaxed mb-6">
                        Tu marketplace de proveedores locales en Florencia, Caqueta. Conectamos negocios con clientes, impulsando el comercio local.
                    </p>

                    <!-- Redes sociales con iconos circulares -->
                    <div class="flex items-center gap-3">
                        <a href="https://web.facebook.com/people/Websen/61571549591292/" target="_blank" rel="noopener noreferrer"
                           class="w-10 h-10 rounded-full bg-gray-800 flex items-center justify-center transition-all duration-300 hover:scale-110" style="--hover-bg: #1877F2;">
                            <i class="fab fa-facebook-f text-gray-400"></i>
                        </a>
                        <a href="https://www.instagram.com/websencol/" target="_blank" rel="noopener noreferrer"
                           class="w-10 h-10 rounded-full bg-gray-800 flex items-center justify-center transition-all duration-300 hover:scale-110">
                            <i class="fab fa-instagram text-gray-400"></i>
                        </a>
                        <a href="https://www.linkedin.com/in/websen-col-99459733b/" target="_blank" rel="noopener noreferrer"
                           class="w-10 h-10 rounded-full bg-gray-800 flex items-center justify-center transition-all duration-300 hover:scale-110">
                            <i class="fab fa-linkedin-in text-gray-400"></i>
                        </a>
                        <a href="https://wa.me/573103348519" target="_blank" rel="noopener noreferrer"
                           class="w-10 h-10 rounded-full bg-gray-800 flex items-center justify-center transition-all duration-300 hover:scale-110">
                            <i class="fab fa-whatsapp text-gray-400"></i>
                        </a>
                    </div>
                </div>

                <!-- Enlaces en dos columnas -->
                <div class="grid grid-cols-2 gap-8">
                    <!-- Explorar -->
                    <div>
                        <h3 class="text-white font-semibold text-sm mb-4" style="text-transform: uppercase; letter-spacing: 0.05em;">Explorar</h3>
                        <ul class="space-y-3">
                            <li>
                                <a href="@Url.Action("Principal", "Home")" class="text-gray-400 hover:text-white transition-colors text-sm">
                                    Inicio
                                </a>
                            </li>
                            <li>
                                <a href="@Url.Action("Listar", "Categorias")" class="text-gray-400 hover:text-white transition-colors text-sm">
                                    Categorias
                                </a>
                            </li>
                            <li>
                                <a href="#proveedores-container" class="text-gray-400 hover:text-white transition-colors text-sm">
                                    Proveedores
                                </a>
                            </li>
                            <li>
                                <a href="#ofertas-container" class="text-gray-400 hover:text-white transition-colors text-sm">
                                    Ofertas
                                </a>
                            </li>
                        </ul>
                    </div>

                    <!-- Negocios -->
                    <div>
                        <h3 class="text-white font-semibold text-sm mb-4" style="text-transform: uppercase; letter-spacing: 0.05em;">Negocios</h3>
                        <ul class="space-y-3">
                            <li>
                                <a href="@Url.Action("Index", "Login")" class="text-gray-400 hover:text-white transition-colors text-sm">
                                    Iniciar sesion
                                </a>
                            </li>
                            <li>
                                <a href="@Url.Action("Registrar", "Usuarios")" class="text-gray-400 hover:text-white transition-colors text-sm">
                                    Registrarse
                                </a>
                            </li>
                            <li>
                                <a href="@Url.Action("Tipos", "Membresias")" class="text-gray-400 hover:text-white transition-colors text-sm">
                                    Membresias
                                </a>
                            </li>
                            <li>
                                <a href="@Url.Action("Index", "Analiticas")" class="text-gray-400 hover:text-white transition-colors text-sm">
                                    Analiticas
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Formulario de contacto -->
            <div class="mb-8">
                <h3 class="text-white font-semibold text-sm mb-4" style="text-transform: uppercase; letter-spacing: 0.05em;">Contactanos</h3>
                <form id="dejanos-tu-resenia" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-2 sm:gap-3">
                    <input type="text" name="nombre" placeholder="Nombre"
                        class="rounded-lg bg-gray-800 border border-gray-700 py-2.5 px-3 text-sm text-white placeholder-gray-500 focus:outline-none" required />
                    <input type="email" name="correo" placeholder="Email"
                        class="rounded-lg bg-gray-800 border border-gray-700 py-2.5 px-3 text-sm text-white placeholder-gray-500 focus:outline-none" required />
                    <input type="text" name="mensaje" placeholder="Tu mensaje..."
                        class="rounded-lg bg-gray-800 border border-gray-700 py-2.5 px-3 text-sm text-white placeholder-gray-500 focus:outline-none" required />
                    <button type="submit"
                        class="rounded-lg py-2.5 text-sm font-semibold text-white flex items-center justify-center gap-2" style="background: linear-gradient(to right, #23916A, #1d7a59);">
                        <i class="fas fa-paper-plane text-xs"></i>
                        Enviar
                    </button>
                </form>
                <p id="footer-opinion-success" class="mt-2 hidden text-xs font-medium items-center gap-1" style="color: #23916A;">
                    <i class="fas fa-check-circle"></i>
                    Mensaje enviado correctamente!
                </p>
            </div>

            <!-- Separador -->
            <div class="h-px bg-gray-700 mb-8"></div>

            <!-- Barra inferior -->
            <div class="flex flex-col md:flex-row items-center justify-between gap-4">
                <!-- Contacto directo -->
                <div class="flex flex-wrap items-center justify-center gap-3 sm:gap-6 text-xs sm:text-sm">
                    <a href="tel:3103348519" class="flex items-center gap-2 text-gray-400 hover:text-white transition-colors">
                        <span class="w-8 h-8 rounded-full bg-gray-800 flex items-center justify-center">
                            <i class="fas fa-phone text-xs" style="color: #23916A;"></i>
                        </span>
                        310 3348519
                    </a>
                    <a href="mailto:abastecetecol@gmail.com" class="flex items-center gap-2 text-gray-400 hover:text-white transition-colors">
                        <span class="w-8 h-8 rounded-full bg-gray-800 flex items-center justify-center">
                            <i class="fas fa-envelope text-xs" style="color: #F66623;"></i>
                        </span>
                        abastecetecol@gmail.com
                    </a>
                    <span class="flex items-center gap-2 text-gray-400">
                        <span class="w-8 h-8 rounded-full bg-gray-800 flex items-center justify-center">
                            <i class="fas fa-map-marker-alt text-xs text-gray-500"></i>
                        </span>
                        Florencia, Caqueta
                    </span>
                </div>

                <!-- Copyright -->
                <p class="text-sm text-gray-500">
                    © @DateTime.Now.Year <span style="color: #23916A;" class="font-medium">Abastecete</span>. Todos los derechos reservados.
                </p>
            </div>
        </div>
    </div>
</footer>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const form = document.getElementById("dejanos-tu-resenia");
        const successMsg = document.getElementById("footer-opinion-success");
        const submitBtn = form.querySelector("button[type='submit']");
        const originalBtnHTML = submitBtn.innerHTML;

        form.addEventListener("submit", async function (e) {
            e.preventDefault();

            const formData = new FormData(form);

            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin text-xs"></i> Enviando...';

            try {
                const response = await fetch("/api/contact/opinion", {
                    method: "POST",
                    body: formData
                });

                const result = await response.json();

                if (result.success) {
                    successMsg.classList.remove("hidden");
                    successMsg.style.display = "flex";
                    form.reset();
                    setTimeout(() => {
                        successMsg.classList.add("hidden");
                        successMsg.style.display = "none";
                    }, 5000);
                } else {
                    alert("Error al enviar tu mensaje.");
                }
            } catch (err) {
                alert("Hubo un error al procesar tu solicitud.");
            } finally {
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalBtnHTML;
            }
        });
    });
</script>
